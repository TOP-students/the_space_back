# The Space - Real-time Social Platform

**The Space** — это современная социальная платформа для общения в реальном времени, построенная на FastAPI и Socket.IO. Проект поддерживает создание пространств (spaces), приватные и групповые чаты, систему ролей и прав доступа, с оптимизированной производительностью для работы с большими объемами данных.

## 🌟 Основные возможности

### Коммуникация
- **Real-time коммуникация** через WebSocket (Socket.IO)
- **Приватные чаты** между пользователями
- **Пространства (Spaces)** с групповыми чатами
- **Реакции на сообщения** (эмодзи)
- **Вложения файлов** (изображения, документы)
- **Упоминания пользователей** (@mentions) с автодополнением
- **Редактирование и удаление** сообщений

### Управление
- **Система ролей** с гибкими правами доступа (RBAC)
- **Управление участниками** (кик, бан, назначение ролей)
- **Настройки пространств** (аватар, баннер, описание)
- **Профили пользователей** с мини-профилями при наведении

### Статусы и активность
- **Статусы пользователей** в реальном времени (В сети, Отошёл, Не беспокоить, Не в сети)
- **WebSocket broadcast** для мгновенного обновления статусов
- **Heartbeat механизм** для отслеживания активности

### Уведомления
- **Система уведомлений** (упоминания, приглашения, изменения ролей)
- **Счётчик непрочитанных** уведомлений
- **Кеширование уведомлений** для быстрой загрузки

### Производительность
- **Виртуализация сообщений** - рендеринг только видимых элементов (для чатов с 50+ сообщениями)
- **Lazy loading** - подгрузка старых сообщений при скролле
- **Кеширование данных** - участники, роли, уведомления кешируются на 30 секунд
- **Параллельная загрузка** - одновременная загрузка данных при инициализации
- **Оптимизированные SQL запросы** - устранение N+1 проблем через JOIN операции
- **Batch загрузка реакций** - все реакции загружаются одним запросом

## 📁 Структура проекта

```
the_space_back/
├── main.py                    # Точка входа, инициализация приложения
├── requirements.txt.backend   # Зависимости бэкенда
├── render.yaml                # Конфигурация для деплоя на Render
│
├── MIN/                       # Frontend (статический)
│   ├── chat.html             # Главная страница чата
│   ├── login.html            # Страница входа
│   ├── register.html         # Страница регистрации
│   ├── css/                  # Стили
│   │   ├── stylechat.css     # Основные стили чата
│   │   ├── modal.css         # Стили модальных окон
│   │   ├── attachments.css   # Стили для вложений
│   │   └── emoji-picker.css  # Стили эмодзи-пикера
│   └── js/                   # JavaScript модули
│       ├── chat.js           # Основная логика чата (с оптимизациями)
│       ├── api.js            # API клиент
│       ├── websocket.js      # WebSocket клиент
│       ├── auth.js           # Аутентификация
│       ├── modal.js          # Модальные окна
│       ├── mini-profile.js   # Мини-профили при наведении
│       ├── attachments.js    # Обработка вложений
│       └── emoji-picker.js   # Эмодзи-пикер
│
├── models/                    # SQLAlchemy модели базы данных
│   ├── base.py               # Все модели (User, Space, Chat, Message, Role, Ban, etc.)
│   └── permissions.py        # Система прав доступа
│
├── schemas/                   # Pydantic схемы для валидации
│   ├── user.py               # UserCreate, UserOut, ProfileOut
│   ├── message.py            # MessageCreate, MessageOut
│   ├── space.py              # SpaceCreate, SpaceOut
│   ├── profile.py            # ProfileUpdate, MyProfileOut
│   └── attachment.py         # AttachmentOut
│
├── routers/                   # API endpoints (FastAPI роутеры)
│   ├── auth.py               # Регистрация, логин
│   ├── spaces.py             # Управление пространствами, участниками, банами
│   ├── messages.py           # Сообщения (оптимизировано)
│   ├── profile.py            # Профили пользователей
│   ├── status.py             # Статусы пользователей
│   ├── notifications.py      # Уведомления
│   ├── roles.py              # Управление ролями
│   └── stickers.py           # Стикеры (если используется)
│
├── crud/                      # Repository pattern для работы с БД
│   ├── user.py               # UserRepository
│   ├── space.py              # SpaceRepository
│   ├── message.py            # MessageRepository (оптимизировано)
│   ├── reaction.py           # ReactionRepository (batch загрузка)
│   ├── activity.py           # ActivityRepository (статусы)
│   ├── role.py               # RoleRepository
│   ├── ban.py                # BanRepository
│   └── notification.py       # NotificationRepository
│
└── utils/                     # Вспомогательные модули
    ├── auth.py               # JWT, get_current_user, update_user_activity_middleware
    ├── socketio_instance.py  # Глобальный экземпляр Socket.IO
    ├── socketio_handlers.py   # Обработчики WebSocket событий
    ├── file_upload.py        # Загрузка файлов в Supabase Storage
    └── storage.py            # Работа с Supabase Storage
```

## 🛠 Технологический стек

### Backend
- **FastAPI** (Python 3.13+) - современный асинхронный веб-фреймворк
- **PostgreSQL** - реляционная база данных
- **SQLAlchemy 2.0** - ORM с оптимизированными запросами (JOIN, batch loading)
- **Socket.IO** (python-socketio) - real-time коммуникация
- **JWT** (python-jose) - аутентификация
- **Pydantic v2** - валидация данных
- **Uvicorn** - ASGI сервер

### Frontend
- **Vanilla JavaScript** - без фреймворков для максимальной производительности
- **Socket.IO Client** - real-time обновления
- **CSS3** - современные стили с поддержкой темной темы
- **LocalStorage** - кеширование настроек пользователя

### Хранилище
- **Supabase Storage** - хранение файлов (аватары, баннеры)
- **Cloudinary** - работа с вложениями

## 🚀 Быстрый старт

### Требования

- Python 3.13+ (или 3.11+ если есть проблемы с psycopg2-binary)
- PostgreSQL 13+
- Supabase аккаунт (для хранения файлов)
- Cloudinary аккаунт
- pip

### Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/TOP-students/the_space_back.git
cd the_space_back
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt.backend
```

4. **Настройте переменные окружения** (создайте `.env`):
```env
SECRET_KEY=your-secret-key-here
DATABASE_URL=postgresql://user:password@localhost/the_space_db
SUPABASE_URL=your-supabase-url
SUPABASE_KEY=your-supabase-key
```

5. **Создайте базу данных PostgreSQL:**
```bash
createdb the_space_db
```

6. **Запустите бэкенд:**
```bash
python main.py
```

7. **Запустите фронтенд** (в отдельном терминале):
```bash
cd MIN
python -m http.server 3000
```

**Бэкенд:** http://localhost:8080  
**Фронтенд:** http://localhost:3000  
**API документация:** http://localhost:8080/docs


## 🏗 Архитектура

### REST API + WebSocket

Приложение использует гибридную архитектуру:
- **REST API** (FastAPI) для CRUD операций
- **WebSocket** (Socket.IO) для real-time обновлений (сообщения, статусы, уведомления)

### Оптимизация производительности

#### Backend оптимизации:
- **Устранение N+1 запросов** - использование JOIN операций вместо множественных запросов
- **Batch загрузка** - загрузка всех реакций одним запросом
- **Оптимизированные запросы** - `get_participants`, `get_messages`, `get_all_spaces` используют JOIN

#### Frontend оптимизации:
- **Виртуализация сообщений** - рендеринг только видимых элементов (для чатов с 50+ сообщениями)
- **Lazy loading** - подгрузка старых сообщений при скролле вверх
- **Кеширование** - участники, роли, уведомления кешируются на 30 секунд
- **Параллельная загрузка** - spaces и notifications загружаются одновременно
- **Умный скролл** - автоматический скролл только если пользователь был внизу

### Асинхронность

Все операции с БД полностью асинхронные:
- `Session` для работы с PostgreSQL
- `async/await` во всех handlers
- Оптимизированные запросы с `joinedload` и `outerjoin`

### Безопасность

- JWT токены с истечением срока действия
- Bcrypt хеширование паролей
- RBAC система прав доступа
- Проверка банов при входе в пространства
- Валидация всех входных данных через Pydantic

## 📝 API Endpoints

### Authentication (`/auth`)
- `POST /auth/register` - Регистрация нового пользователя
- `POST /auth/token` - Вход (получение JWT токена)
- `GET /auth/me` - Получить текущего пользователя

### Spaces (`/spaces`)
- `GET /spaces` - Получить все пространства пользователя (оптимизировано)
- `POST /spaces` - Создать пространство
- `GET /spaces/{space_id}` - Получить информацию о пространстве
- `POST /spaces/{space_id}/join` - Присоединиться к пространству
- `GET /spaces/{space_id}/participants` - Список участников (оптимизировано, включает статусы)
- `GET /spaces/{space_id}/roles` - Список ролей пространства
- `GET /spaces/{space_id}/my-permissions` - Мои разрешения в пространстве
- `POST /spaces/{space_id}/kick/{user_id}` - Исключить участника
- `POST /spaces/{space_id}/ban/{user_id}` - Забанить пользователя
- `POST /spaces/{space_id}/unban/{user_id}` - Разбанить пользователя

### Messages (`/messages`)
- `GET /messages/{chat_id}` - Получить сообщения (с пагинацией, оптимизировано)
- `POST /messages/{chat_id}` - Отправить сообщение
- `PATCH /messages/{message_id}` - Редактировать сообщение
- `DELETE /messages/{message_id}` - Удалить сообщение
- `POST /messages/{message_id}/reaction` - Добавить/удалить реакцию

### Profile (`/profile`)
- `GET /profile/me` - Получить свой профиль
- `PATCH /profile/me` - Обновить свой профиль
- `GET /profile/{user_id}` - Получить профиль пользователя (с актуальным статусом)
- `POST /profile/upload-avatar` - Загрузить аватар
- `POST /profile/upload-banner` - Загрузить баннер

### Status (`/status`)
- `GET /status/my-status` - Получить мой статус
- `POST /status/set-status` - Установить статус (online, away, dnd, offline)
- `GET /status/user/{user_id}` - Получить статус пользователя
- `POST /status/heartbeat` - Обновить активность (без изменения статуса)

### Notifications (`/notifications`)
- `GET /notifications` - Получить уведомления (с кешированием)
- `GET /notifications/unread-count` - Количество непрочитанных
- `POST /notifications/{notification_id}/read` - Пометить как прочитанное
- `POST /notifications/read-all` - Пометить все как прочитанные

### Roles (`/spaces/{space_id}/roles`)
- `GET /spaces/{space_id}/roles` - Список ролей
- `POST /spaces/{space_id}/roles` - Создать роль
- `PATCH /spaces/{space_id}/roles/{role_id}` - Обновить роль
- `DELETE /spaces/{space_id}/roles/{role_id}` - Удалить роль
- `POST /spaces/{space_id}/assign-role/{user_id}/{role_id}` - Назначить роль

## 🔌 Socket.IO Events

### Client → Server:
- `connect` - Подключение (с user_id и nickname в query)
- `join_room` - Присоединиться к чату
- `leave_room` - Покинуть чат
- `send_message` - Отправить сообщение
- `edit_message` - Редактировать сообщение
- `delete_message` - Удалить сообщение
- `add_reaction` - Добавить реакцию

### Server → Client:
- `connected` - Подтверждение подключения
- `joined_room` - Подтверждение присоединения к комнате
- `user_joined_room` - Пользователь присоединился к чату
- `user_left_room` - Пользователь покинул чат
- `new_message` - Новое сообщение в чате
- `message_edited` - Сообщение отредактировано
- `message_deleted` - Сообщение удалено
- `reaction_added` - Реакция добавлена
- `reaction_removed` - Реакция удалена
- `user_status_changed` - Статус пользователя изменился (broadcast всем)
- `user_kicked` - Пользователь исключен из пространства

## ⚡ Оптимизации производительности

### Backend
- **Устранение N+1 запросов:**
  - `get_participants` - один запрос с JOIN для участников и ролей
  - `get_messages` - batch загрузка всех реакций одним запросом
  - `get_all_spaces` - JOIN с Chat для получения chat_id

- **Оптимизация запросов:**
  - Использование `joinedload` для загрузки связанных данных
  - Batch загрузка банов для всех пользователей
  - Оптимизированная загрузка реакций через `get_reactions_for_messages`

### Frontend
- **Виртуализация сообщений:**
  - Для чатов с 50+ сообщениями рендерится только видимая область
  - При скролле автоматически подгружаются/удаляются невидимые элементы
  - Эффект: **10-20x ускорение** для больших чатов

- **Lazy loading:**
  - Загружаются только последние 30 сообщений при открытии чата
  - Старые сообщения подгружаются при скролле вверх
  - Эффект: **40-60% ускорение** открытия чата

- **Кеширование:**
  - Участники и роли кешируются на 30 секунд
  - Уведомления кешируются на 30 секунд
  - Эффект: **70-80% ускорение** повторного открытия

- **Параллельная загрузка:**
  - Spaces и notifications загружаются одновременно
  - Эффект: **30-50% ускорение** начальной загрузки

## 🎨 Функции интерфейса

- **Темная/светлая тема** с сохранением в localStorage
- **Цветовые схемы** (бордовая, синяя, зеленая, фиолетовая, оранжевая, бирюзовая)
- **Размер шрифта** (малый, средний, большой)
- **Мини-профили** при наведении на аватары
- **Эмодзи-пикер** для реакций
- **Автодополнение @-упоминаний**
- **Загрузка файлов** с превью
- **Редактирование сообщений** inline

## 🧪 Тестирование

1. Запустите бэкенд: `python main.py`
2. Запустите фронтенд: `cd MIN && python -m http.server 3000`
3. Откройте http://localhost:3000/chat.html
4. Зарегистрируйтесь или войдите
5. Создайте пространство и начните общение

## 📊 Производительность

### До оптимизаций:
- Открытие чата: **12-20 секунд**
- Загрузка правой панели: **20+ секунд**
- Начальная загрузка: **20 секунд**

### После оптимизаций:
- Открытие чата: **3-5 секунд** (загружается 30 сообщений вместо 50)
- Загрузка правой панели: **2-3 секунды** (кеш + параллельные запросы)
- Начальная загрузка: **8-12 секунд** (параллельная загрузка)
- Большие чаты (100+ сообщений): **оч быстро** (виртуализация)

## 🚢 Деплой

### Backend (Render)
- Файл `render.yaml` содержит конфигурацию
- Автоматический деплой при push в main
- PostgreSQL база данных на Render

### Frontend (Netlify/Render)
- Статические файлы из папки `MIN/`
- Конфигурация в `render.yaml`

## 👥 Авторы

Создано в рамках учебного проекта TopAcademy

## 📄 Лицензия

Проект создан в образовательных целях
